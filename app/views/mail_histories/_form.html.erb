<%= form_with(model: mail_history, local: true) do |form| %>
  <% if mail_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mail_history.errors.count, "error") %> prohibited this mail_history from being saved:</h2>

      <ul>
      <% mail_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mail_id %>
    <%= @mail_id%>
    <%#= form.text_field :mail_id %>
  </div>

  <div class="field">
    <%= form.label :staff_id %>
    <%= form.text_field :staff_id %>
  </div>

  <div class="field">
    <%= form.label :status_delivery_id %>
    <%= form.text_field :status_delivery_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
